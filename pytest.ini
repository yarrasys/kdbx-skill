[pytest]
# explicit per-skill/plugin test dirs (not `skills plugins` — that would re-collect
# skill tests through the plugin's bundled-skill symlink). Append a line per new skill.
testpaths = skills/kdbx/tests plugins/kdbx/tests
addopts = -ra
